import {Button, Text, View} from 'react-native';

export const BlogsScreen = ({navigation}: {navigation: any}) => {
  return (
    <View>
      <Text style={{fontSize: 20, fontWeight: 'bold', color: 'red'}}>
        Blogs
      </Text>
    </View>
  );
};
